#include <stdio.h>
#include <string.h>
extern char **environ;

char *_getenv(const char *name)
{
	if ()
	{
	}
	return (printenv);
}
int main(void)
{
	char *envvar = "PATH";
	printf("PATH: %s\n", _getenv(envvar));
	return (0);
}
